apiVersion: v1
kind: ResourceQuota
metadata:
  name: insurance-management-quota
  namespace: insurance-management-homolog
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "10"
    services: "5"
