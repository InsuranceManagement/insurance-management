apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: insurance-management-prod
data:
  PORT: "8080"
  ALLOWED_ORIGINS: "*"
  JWT_EXPIRES_IN_SECONDS: "3600"
  RESET_PASSWORD_TOKEN_TTL_MINUTES: "30"
